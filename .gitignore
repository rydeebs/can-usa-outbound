# Environment — NEVER commit this
.env
*.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyc
*.pyo
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Data files — contain personal contact info, keep local
data/contacts.json
data/seq_emails.json
data/reply_log.json
data/*.json

# Logs
agent.log
*.log

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Lock file (runtime only)
data/.contacts.lock

# Auth tokens — contain Pawel's credentials, never commit
agent/token_cache.json
token_cache.json
agent/token_google.json
token_google.json
# Google OAuth credentials file — contains client secret, never commit
agent/google_credentials.json
google_credentials.json